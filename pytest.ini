[pytest]
# Conftest sets DJANGO_SETTINGS_MODULE=config.settings_test 
addopts = -v --tb=short

pythonpath = src
testpaths = src/tests

markers =
    django_db: Mark a test to use the Django database (pytest-django).
